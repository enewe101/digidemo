$(document).ready(resend_letter_init);

function resend_letter_init() {
	$('.resend_letter_form_area').each(function(i) {
		submit_letter = $('.submit_letter', $(this));

		submit_letter.click(function() {
		});
	})
}
